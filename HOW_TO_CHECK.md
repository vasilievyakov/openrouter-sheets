# 🔍 Как проверить, работает ли система

## ✅ Признаки того, что система работает:

### 1. В Google Sheets (то, что вы видите):

**Уже есть:**
- ✅ Колонка "Prompt" создана (видно на скриншоте)
- ✅ Заголовок колонки содержит промпт и временную метку
- ✅ Это означает, что Apps Script выполнился успешно

**Должно появиться:**
- 📊 **Новая колонка справа от "Prompt"** с результатами обработки
- Каждая строка должна содержать ответ от LLM (например: "Tesla", "SmartisConf", "Яндекс Директ")

### 2. Проверка GitHub Actions (самый важный шаг):

1. Откройте репозиторий: https://github.com/vasilievyakov/openrouter-sheets
2. Перейдите на вкладку **Actions**
3. Найдите запуск workflow "Process Google Sheet"
4. Если видите:
   - ✅ **Зеленый статус** = работает успешно
   - 🟡 **Желтый статус** = выполняется
   - ❌ **Красный статус** = ошибка (нужно посмотреть логи)

### 3. Как проверить сейчас:

#### Вариант A: Через GitHub Actions (самый надежный)

```
1. Откройте: https://github.com/vasilievyakov/openrouter-sheets/actions
2. Найдите последний запуск workflow
3. Откройте его и посмотрите логи
```

**Что искать в логах:**
- ✅ "🚀 Начало обработки таблицы" - скрипт запустился
- ✅ "📖 Чтение данных из таблицы..." - данные читаются
- ✅ "📦 Батч 1/1" - обработка идет
- ✅ "✨ Обработка завершена!" - все готово
- ❌ Любые ошибки (красным цветом)

#### Вариант B: Через логи Apps Script

```
1. Откройте Google Sheet
2. Extensions → Apps Script
3. View → Logs (или выполните функцию runPrompt вручную)
4. Посмотрите логи отправки запроса
```

**Что искать:**
- ✅ "Запрос отправлен в OpenRouter" - запрос отправлен
- ✅ Статус 200-299 - успешная отправка
- ❌ Ошибки 401, 403, 404 - проблемы с токеном или URL

#### Вариант C: Проверка результата в таблице

Подождите 2-5 минут и проверьте:
- Появилась ли **новая колонка** справа от "Prompt"?
- Заполнены ли **ячейки** с результатами?

### 4. Диагностика проблем:

#### Проблема: Колонка "Prompt" создана, но результатов нет

**Возможные причины:**

1. **GitHub Actions не запустился:**
   - Проверьте Actions в репозитории
   - Если нет запусков = проблема с отправкой запроса
   - Проверьте GITHUB_TOKEN в apps-script/config.gs

2. **GitHub Actions запустился, но завершился с ошибкой:**
   - Откройте Actions → последний запуск → логи
   - Найдите ошибку (обычно красным цветом)
   - Частые ошибки:
     - "OPENROUTER_KEY is required" → не добавлен Secret
     - "Permission denied" → Service Account не имеет доступа
     - "Invalid credentials" → неправильный JSON

3. **GitHub Actions выполняется долго:**
   - Это нормально для больших таблиц
   - Проверьте логи - должен быть прогресс
   - Может занять 5-10 минут для 4000 строк

#### Проблема: Запрос не отправляется из Apps Script

**Проверьте:**
1. GITHUB_TOKEN в apps-script/config.gs:
   ```javascript
   const GITHUB_TOKEN = "ghp_ваш_реальный_токен";
   ```
   Должен начинаться с `ghp_` и не быть плейсхолдером

2. Логи Apps Script:
   - View → Logs
   - Должны быть записи о попытке отправки

### 5. Быстрая проверка прямо сейчас:

Выполните в терминале:

```bash
# Проверка GitHub Actions (через API)
curl https://api.github.com/repos/vasilievyakov/openrouter-sheets/actions/runs?per_page=1
```

Или откройте в браузере:
```
https://github.com/vasilievyakov/openrouter-sheets/actions
```

### 6. Что должно произойти:

**Полный цикл работы:**

```
1. Вы нажали "Обработать новости" ✅ (уже сделано)
   ↓
2. Apps Script создал колонку "Prompt" ✅ (видно на скриншоте)
   ↓
3. Apps Script отправил запрос в GitHub ✅ (нужно проверить)
   ↓
4. GitHub Actions запустился ⏳ (проверьте Actions)
   ↓
5. Node.js скрипт читает данные из таблицы ⏳
   ↓
6. Отправляет запросы в OpenRouter ⏳
   ↓
7. Записывает результаты обратно в таблицу ⏳
   ↓
8. Появляется колонка с результатами ✅ (должно появиться)
```

### 7. Чеклист проверки:

- [ ] Колонка "Prompt" создана (✅ уже есть)
- [ ] Проверить GitHub Actions - есть ли запуски?
- [ ] Если есть запуски - какой статус?
- [ ] Если статус успешный - проверить таблицу через 5 минут
- [ ] Если статус ошибка - прочитать логи и исправить

## 📝 Следующие шаги:

1. **Сейчас:** Откройте https://github.com/vasilievyakov/openrouter-sheets/actions
2. **Проверьте:** Есть ли запуски workflow?
3. **Если есть:** Откройте последний и посмотрите логи
4. **Если нет:** Проверьте GITHUB_TOKEN в Apps Script

Сообщите, что видите в GitHub Actions, и я помогу разобраться!

