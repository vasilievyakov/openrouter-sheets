# 🔧 Подробное руководство по устранению ошибок

## ❌ Ошибка 1: "OPENROUTER_KEY is required"

### Что это означает:
Скрипт не может найти API ключ OpenRouter в GitHub Secrets.

### Как исправить:

#### Шаг 1: Получите API ключ OpenRouter

1. Перейдите на https://openrouter.ai/
2. Зарегистрируйтесь или войдите в аккаунт
3. Перейдите в раздел "Keys" (или "API Keys")
4. Создайте новый API ключ или скопируйте существующий
5. Ключ должен выглядеть примерно так: `sk-or-v1-...` или `sk-or-...`

#### Шаг 2: Добавьте ключ в GitHub Secrets

1. Откройте репозиторий: https://github.com/vasilievyakov/openrouter-sheets
2. Перейдите в **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **New repository secret**
4. Заполните:
   - **Name:** `OPENROUTER_KEY` (точно так, регистр важен!)
   - **Secret:** вставьте ваш API ключ OpenRouter
5. Нажмите **Add secret**

#### Шаг 3: Проверьте

После добавления:
- Убедитесь, что имя секрета точно `OPENROUTER_KEY` (без пробелов, точное совпадение)
- Попробуйте запустить workflow снова

---

## ❌ Ошибка 2: "GOOGLE_APPLICATION_CREDENTIALS invalid" или "Failed to read credentials file"

### Что это означает:
JSON файл Service Account невалидный или неправильно добавлен в Secrets.

### Как исправить:

#### Шаг 1: Получите JSON файл Service Account

1. Перейдите в [Google Cloud Console](https://console.cloud.google.com/)
2. Выберите ваш проект (или создайте новый)
3. Включите Google Sheets API:
   - **APIs & Services** → **Library**
   - Найдите "Google Sheets API"
   - Нажмите **Enable**
4. Создайте Service Account:
   - **IAM & Admin** → **Service Accounts**
   - Нажмите **Create Service Account**
   - Название: `sheets-openrouter`
   - Нажмите **Create and Continue**
   - Роль: **Editor** (или нажмите **Continue** без роли)
   - Нажмите **Done**
5. Создайте ключ:
   - Откройте созданный Service Account
   - Перейдите во вкладку **Keys**
   - Нажмите **Add Key** → **Create new key**
   - Выберите **JSON**
   - Нажмите **Create**
   - JSON файл скачается автоматически

#### Шаг 2: Откройте JSON файл

Откройте скачанный файл в текстовом редакторе. Он должен выглядеть примерно так:

```json
{
  "type": "service_account",
  "project_id": "ваш-проект-id",
  "private_key_id": "...",
  "private_key": "-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n",
  "client_email": "sheets-openrouter@ваш-проект.iam.gserviceaccount.com",
  "client_id": "...",
  "auth_uri": "https://accounts.google.com/o/oauth2/auth",
  "token_uri": "https://oauth2.googleapis.com/token",
  ...
}
```

#### Шаг 3: Скопируйте весь JSON как одну строку

**Важно:** JSON должен быть в одной строке!

1. Откройте JSON файл
2. Скопируйте **весь** файл (Ctrl+A, Ctrl+C)
3. В Notepad++ или другом редакторе можно удалить все переносы строк:
   - Нажмите Ctrl+H (замена)
   - Найдите: `\n` (перенос строки)
   - Замените на: пробел или ничего
   - Или просто скопируйте весь текст - GitHub примет многострочный JSON

#### Шаг 4: Добавьте в GitHub Secrets

1. Откройте репозиторий: https://github.com/vasilievyakov/openrouter-sheets
2. **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **New repository secret**
4. Заполните:
   - **Name:** `GOOGLE_APPLICATION_CREDENTIALS` (точно так!)
   - **Secret:** вставьте **весь JSON** файл (всю строку или весь текст)
5. Нажмите **Add secret**

#### Шаг 5: Поделитесь Google Sheet с Service Account

1. Откройте ваш Google Sheet
2. Нажмите кнопку **Поделиться** (Share)
3. Найдите в JSON файле поле `client_email` (например: `sheets-openrouter@проект.iam.gserviceaccount.com`)
4. Вставьте этот email в поле "Добавить людей и группы"
5. Выберите права: **Редактор** (Editor)
6. Нажмите **Отправить** (Send)

#### Шаг 6: Проверьте

После добавления:
- Убедитесь, что имя секрета точно `GOOGLE_APPLICATION_CREDENTIALS`
- Убедитесь, что JSON файл скопирован полностью
- Убедитесь, что Google Sheet поделен с email из `client_email`
- Попробуйте запустить workflow снова

**Проверка валидности JSON:**
- JSON должен начинаться с `{` и заканчиваться `}`
- Все поля должны быть в кавычках
- Не должно быть лишних запятых в конце

---

## ❌ Ошибка 3: "Permission denied" или "The caller does not have permission"

### Что это означает:
Service Account не имеет доступа к Google Sheet или таблица не существует.

### Как исправить:

#### Шаг 1: Проверьте email Service Account

1. Откройте файл JSON Service Account
2. Найдите поле `client_email`
3. Скопируйте email (например: `sheets-openrouter@проект.iam.gserviceaccount.com`)

#### Шаг 2: Поделитесь Google Sheet

1. Откройте ваш Google Sheet
2. Нажмите кнопку **Поделиться** (Share) в правом верхнем углу
3. В поле "Добавить людей и группы" вставьте email Service Account
4. **Важно:** Выберите права **Редактор** (Editor), а не "Читатель"!
5. Нажмите **Отправить** (Send)

#### Шаг 3: Проверьте правильность ID таблицы

1. Откройте Google Sheet
2. Посмотрите на URL в браузере:
   ```
   https://docs.google.com/spreadsheets/d/SPREADSHEET_ID/edit
   ```
3. Скопируйте `SPREADSHEET_ID` (часть между `/d/` и `/edit`)
4. Убедитесь, что в Apps Script правильно передается этот ID

#### Шаг 4: Проверьте название листа

1. Откройте Google Sheet
2. Внизу страницы посмотрите название листа (например: "Sheet1", "Лист1")
3. Убедитесь, что название совпадает с тем, что передается в скрипт

#### Шаг 5: Проверьте доступ через тест

Можете протестировать доступ локально:

```bash
# Установите переменные окружения
$env:GOOGLE_APPLICATION_CREDENTIALS = "путь\к\файлу.json"

# Запустите тест
npm run test:sheets SPREADSHEET_ID "Sheet1"
```

Если тест проходит успешно, значит доступ есть.

#### Шаг 6: Альтернативные решения

Если все еще не работает:

1. **Проверьте, что Service Account активен:**
   - Google Cloud Console → IAM & Admin → Service Accounts
   - Убедитесь, что аккаунт не отключен

2. **Проверьте, что API включен:**
   - Google Cloud Console → APIs & Services → Enabled APIs
   - Убедитесь, что "Google Sheets API" включен

3. **Попробуйте создать новый Service Account:**
   - Иногда старые аккаунты имеют проблемы с правами
   - Создайте новый и повторите все шаги

---

## ✅ Проверка всех настроек

После исправления ошибок, проверьте:

### Чеклист GitHub Secrets:

- [ ] `OPENROUTER_KEY` добавлен (начинается с `sk-or-`)
- [ ] `GOOGLE_APPLICATION_CREDENTIALS` добавлен (весь JSON файл)
- [ ] Имена секретов точно совпадают (регистр важен!)

### Чеклист Google Sheets:

- [ ] Google Sheet поделен с email Service Account
- [ ] Права: **Редактор** (Editor)
- [ ] Google Sheets API включен в проекте
- [ ] Service Account активен

### Чеклист Apps Script:

- [ ] `GITHUB_TOKEN` установлен в `config.gs`
- [ ] `WEBHOOK_URL` правильный
- [ ] Код скопирован в Apps Script редактор

---

## 🧪 Тестирование после исправления

После исправления всех ошибок:

1. **Локальный тест:**
   ```bash
   npm run check
   npm run test:openrouter
   npm run test:sheets SPREADSHEET_ID "Sheet1"
   ```

2. **Тест через Google Sheets:**
   - Откройте Google Sheet
   - LLM Tools → Обработать новости
   - Введите промпт
   - Проверьте GitHub Actions

3. **Проверка логов:**
   - Откройте последний запуск в GitHub Actions
   - Убедитесь, что все шаги зеленые
   - Проверьте логи на наличие ошибок

---

## 📞 Если все еще не работает

Если после всех исправлений проблема остается:

1. Скопируйте **полный текст ошибки** из логов GitHub Actions
2. Укажите, на каком шаге происходит ошибка
3. Проверьте, что все Secrets добавлены правильно

Часто проблема в том, что:
- Secret имеет неправильное имя (проверьте регистр!)
- JSON файл скопирован не полностью
- Google Sheet не поделен с правильным email

