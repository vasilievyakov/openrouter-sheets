# ✅ Проверка: Что нужно изменить в Apps Script

## 📋 Текущее состояние Apps Script:

### ✅ Что уже правильно:

1. **main.gs** - код правильный, отправляет `repository_dispatch` корректно
2. **config.gs** - `WEBHOOK_URL` настроен правильно

### ⚠️ Что нужно проверить/изменить:

1. **GITHUB_TOKEN в config.gs**
   - Должен быть реальный токен (не плейсхолдер)
   - Должен начинаться с `ghp_`
   - Должен иметь права `repo`

## 🔍 Что проверить в Apps Script:

### Шаг 1: Откройте Apps Script

1. Откройте Google Sheet
2. **Extensions → Apps Script**

### Шаг 2: Проверьте config.gs

Откройте файл `config.gs` и проверьте:

```javascript
// Должно быть:
const WEBHOOK_URL = "https://api.github.com/repos/vasilievyakov/openrouter-sheets/dispatches";
const GITHUB_TOKEN = "ghp_ваш_реальный_токен";  // ← ВАЖНО: реальный токен!
```

**Проверьте:**
- ✅ `WEBHOOK_URL` правильный (уже настроен)
- ❓ `GITHUB_TOKEN` - это реальный токен или плейсхолдер?

### Шаг 3: Если GITHUB_TOKEN - плейсхолдер

Если в `config.gs` написано:
```javascript
const GITHUB_TOKEN = "ghp_YOUR_PERSONAL_ACCESS_TOKEN_HERE";
```

То нужно заменить на реальный токен:
```javascript
const GITHUB_TOKEN = "ghp_ваш_реальный_токен_здесь";
```

### Шаг 4: Сохраните и обновите

1. Сохраните проект (Ctrl+S)
2. Обновите страницу Google Sheet (F5)
3. Проверьте, что меню "LLM Tools" появляется

## ✅ Итоговый чеклист:

- [ ] `WEBHOOK_URL` правильный (уже настроен) ✅
- [ ] `GITHUB_TOKEN` заменен на реальный токен ❓
- [ ] Проект сохранен
- [ ] Google Sheet обновлен
- [ ] Меню "LLM Tools" появляется

## 🚀 После проверки:

Если `GITHUB_TOKEN` уже настроен - ничего менять не нужно!

Просто запустите workflow снова из Google Sheets - должен работать с новым workflow файлом.

## 💡 Если токен не настроен:

1. Получите токен: https://github.com/settings/tokens
2. Создайте новый токен с правами `repo`
3. Скопируйте токен (начинается с `ghp_`)
4. Вставьте в `config.gs` вместо плейсхолдера
5. Сохраните

## 📝 Резюме:

**Скорее всего менять ничего не нужно**, если:
- ✅ `WEBHOOK_URL` правильный (он уже настроен)
- ✅ `GITHUB_TOKEN` уже заменен на реальный токен

Но **обязательно проверьте** `GITHUB_TOKEN` - он должен быть реальным токеном, а не плейсхолдером!

