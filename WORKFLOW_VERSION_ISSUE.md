# 🔍 Проблема: GitHub Actions использует старую версию workflow

## Возможные причины:

1. **Workflow файл читается из старого коммита** - но мы уже добавили `ref: main`
2. **GitHub Actions кеширует workflow** - может быть проблема с кешем
3. **Apps Script может отправлять ref** - но обычно repository_dispatch использует HEAD

## 🔧 Решение: Принудительно использовать последний коммит

Можем добавить явное указание SHA последнего коммита в checkout, или использовать другой подход.

Но сначала проверим - может быть проблема в том, что workflow файл читается из старого коммита при создании события.

## 💡 Альтернативное решение:

Можем использовать `workflow_dispatch` вместо `repository_dispatch`, но это требует изменения Apps Script.

Или можем попробовать добавить параметр в repository_dispatch для указания ref.

Но проще всего - убедиться, что workflow файл точно правильный на GitHub и перезапустить.

