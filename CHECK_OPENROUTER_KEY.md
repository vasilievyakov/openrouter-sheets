# Проверка и исправление ошибки 401 OpenRouter API

## Проблема

Если вы видите ошибку `401 - No cookie auth credentials found` в Google Sheets, это означает, что ключ OpenRouter API недействителен или не установлен правильно.

## Пошаговая инструкция

### 1. Проверьте ключ на сайте OpenRouter

1. Откройте https://openrouter.ai/keys
2. Войдите в свой аккаунт
3. Проверьте список ваших ключей:
   - Убедитесь, что ключ существует
   - Проверьте, что ключ не истек
   - Проверьте, что на балансе есть средства

### 2. Проверьте формат ключа

Ключи OpenRouter обычно начинаются с:
- `sk-or-v1-` (для новых ключей)
- `sk-` (для старых ключей)

Пример правильного ключа:
```
sk-or-v1-1234567890abcdef1234567890abcdef1234567890abcdef1234567890abcdef
```

### 3. Обновите ключ в GitHub Secrets

1. Откройте ваш репозиторий на GitHub
2. Перейдите в **Settings** → **Secrets and variables** → **Actions**
3. Найдите секрет `OPENROUTER_KEY`
4. Нажмите **Update** (или создайте новый, если его нет)
5. **ВАЖНО:** Скопируйте ключ ТОЧНО, без лишних пробелов:
   - Не добавляйте пробелы в начале или конце
   - Не добавляйте переносы строк
   - Скопируйте ключ полностью, от начала до конца
6. Нажмите **Update secret**

### 4. Проверьте, что ключ правильно установлен

После обновления ключа, запустите workflow еще раз и проверьте логи:

1. В шаге "Process Sheet" должны быть следующие строки:
   ```
   🔑 OPENROUTER_KEY проверка:
      - Установлен: ✅
      - Длина: XX символов
      - Начинается с: sk-...
   ```

2. Если вы видите ошибку 401, проверьте логи на наличие:
   ```
   ❌ ОШИБКА АВТОРИЗАЦИИ (401):
      Ключ (превью): sk-or-v1-...XXXX
   ```

### 5. Распространенные ошибки

#### ❌ Неправильно: ключ с пробелами
```
sk-or-v1- 1234567890abcdef...
```

#### ✅ Правильно: ключ без пробелов
```
sk-or-v1-1234567890abcdef...
```

#### ❌ Неправильно: ключ с переносами строк
```
sk-or-v1-1234567890abcdef
1234567890abcdef...
```

#### ✅ Правильно: ключ в одну строку
```
sk-or-v1-1234567890abcdef1234567890abcdef...
```

### 6. Тестирование ключа локально (опционально)

Если хотите проверить ключ локально, создайте файл `test-key.js`:

```javascript
import fetch from "node-fetch";

const OPENROUTER_KEY = "ВАШ_КЛЮЧ_ЗДЕСЬ";

async function testKey() {
  try {
    const res = await fetch("https://openrouter.ai/api/v1/chat/completions", {
      method: "POST",
      headers: {
        "Authorization": `Bearer ${OPENROUTER_KEY}`,
        "Content-Type": "application/json",
      },
      body: JSON.stringify({
        model: "openai/gpt-4o-mini",
        messages: [
          { role: "user", content: "Привет!" }
        ]
      })
    });

    if (!res.ok) {
      const error = await res.text();
      console.error("❌ Ошибка:", res.status, error);
    } else {
      const data = await res.json();
      console.log("✅ Ключ работает! Ответ:", data.choices[0].message.content);
    }
  } catch (error) {
    console.error("❌ Ошибка сети:", error.message);
  }
}

testKey();
```

Запустите:
```bash
node test-key.js
```

### 7. Если проблема сохраняется

Если после всех проверок ошибка 401 все еще возникает:

1. **Создайте новый ключ** на https://openrouter.ai/keys
2. **Обновите секрет** в GitHub Secrets
3. **Убедитесь, что на балансе есть средства** (минимум $1-2 для тестирования)
4. **Проверьте логи GitHub Actions** на наличие дополнительных ошибок

## Контакты для поддержки

- OpenRouter: https://openrouter.ai/docs
- GitHub Issues: откройте issue в вашем репозитории

