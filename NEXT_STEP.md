# 🔧 Попробуем другой подход

## Изменение:

Заменил `setup-node@v4` на `setup-node@v3` для большей стабильности.

## 🔄 Что делать:

1. Подождите минуту (чтобы изменения применились в GitHub)
2. Откройте Google Sheet
3. Запустите снова: **LLM Tools → Обработать новости**
4. Проверьте GitHub Actions

Если это не поможет, следующий вариант - использовать встроенный Node.js в runner или установить Node.js вручную через curl.

## 📋 Альтернативный вариант (если v3 не поможет):

Можем попробовать установить Node.js вручную:

```yaml
- name: Setup Node.js manually
  run: |
    curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
    sudo apt-get install -y nodejs
```

Но сначала попробуйте v3 - это должно помочь! 🚀

