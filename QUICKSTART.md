# 🚀 Быстрый старт

## Автоматическая проверка конфигурации

После установки зависимостей запустите проверку:

```bash
npm install
npm run check
```

Этот скрипт проверит:
- ✅ Установлены ли все переменные окружения
- ✅ Правильность конфигурации Apps Script
- ✅ Наличие всех необходимых файлов
- ✅ Валидность credentials

## Тестирование подключений

### Тест OpenRouter API

```bash
# Установите переменную окружения
export OPENROUTER_KEY="your-key"

# Запустите тест
npm run test:openrouter
```

### Тест Google Sheets API

```bash
# Установите переменную окружения
export GOOGLE_APPLICATION_CREDENTIALS="./path/to/credentials.json"

# Запустите тест (с ID таблицы)
npm run test:sheets SPREADSHEET_ID "Sheet1"

# Или без ID (только проверка credentials)
npm run test:sheets
```

## Локальное тестирование полного цикла

После настройки всех компонентов можно протестировать локально:

```bash
# Установите переменные окружения
export OPENROUTER_KEY="your-key"
export GOOGLE_APPLICATION_CREDENTIALS='{"type":"service_account",...}'

# Запустите обработку
node index.js "SPREADSHEET_ID" "Sheet1" "Translate this into English" 2
```

Где:
- `SPREADSHEET_ID` — ID вашей таблицы (из URL)
- `Sheet1` — название листа
- `"Translate this into English"` — промпт
- `2` — номер колонки для записи результатов

## Использование .env файла (опционально)

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Заполните значения и используйте с `dotenv` (если установлен):

```bash
# Установите dotenv
npm install dotenv

# Используйте в скриптах
node -r dotenv/config index.js ...
```

## Все доступные команды

```bash
npm run check           # Проверка конфигурации
npm run test:openrouter # Тест OpenRouter API
npm run test:sheets     # Тест Google Sheets API
npm start               # Запуск основного скрипта
```

## Решение проблем

Если что-то не работает:

1. **Запустите проверку конфигурации:**
   ```bash
   npm run check
   ```

2. **Проверьте подключения отдельно:**
   ```bash
   npm run test:openrouter
   npm run test:sheets
   ```

3. **Проверьте логи:**
   - GitHub Actions: репозиторий → Actions
   - Apps Script: View → Logs в редакторе

4. **Следуйте инструкциям в SETUP.md**

