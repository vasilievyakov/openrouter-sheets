# 🧪 Пошаговое тестирование системы

## Шаг 1: Установка переменных окружения

Установите переменные окружения для тестирования. В PowerShell:

```powershell
# OpenRouter API ключ
$env:OPENROUTER_KEY = "your-openrouter-key-here"

# Google Service Account credentials (два варианта):

# Вариант 1: Путь к JSON файлу
$env:GOOGLE_APPLICATION_CREDENTIALS = "C:\path\to\your\credentials.json"

# Вариант 2: JSON строка (скопируйте весь JSON из файла)
$env:GOOGLE_APPLICATION_CREDENTIALS = '{"type":"service_account","project_id":"..."}'
```

## Шаг 2: Проверка конфигурации

```bash
npm run check
```

Должно показать ✅ для всех проверок.

## Шаг 3: Тест OpenRouter API

```bash
npm run test:openrouter
```

Ожидаемый результат:
- ✅ Подключение успешно
- ✅ Ответ от модели получен

## Шаг 4: Тест Google Sheets API

```bash
# Укажите ID вашей тестовой таблицы
npm run test:sheets SPREADSHEET_ID "Sheet1"
```

Или только проверка credentials:
```bash
npm run test:sheets
```

Ожидаемый результат:
- ✅ Credentials загружены
- ✅ Подключение к таблице успешно
- ✅ Права на чтение/запись подтверждены

## Шаг 5: Локальное тестирование полного цикла

```bash
node index.js "SPREADSHEET_ID" "Sheet1" "Translate this into English" 2
```

Где:
- `SPREADSHEET_ID` - ID вашей таблицы (из URL)
- `Sheet1` - название листа
- `"Translate this into English"` - тестовый промпт
- `2` - номер колонки для записи результатов

## Шаг 6: Тест через Google Sheets

1. Откройте Google Sheet с тестовыми данными (10-20 строк в столбце A)
2. Extensions → Apps Script
3. Убедитесь, что код из `apps-script/main.gs` и `apps-script/config.gs` скопирован
4. В `config.gs` должен быть указан ваш `GITHUB_TOKEN`
5. Сохраните проект (Ctrl+S)
6. Обновите страницу Google Sheet
7. В меню выберите **LLM Tools → Обработать новости**
8. Введите промпт: `Translate this into English`
9. Нажмите OK
10. Проверьте GitHub Actions:
    - Перейдите в репозиторий → Actions
    - Должен запуститься workflow "Process Google Sheet"
    - Проверьте логи выполнения
11. Через несколько минут проверьте Google Sheet - должна появиться новая колонка с результатами

## Устранение проблем

### Ошибка: "OPENROUTER_KEY не установлен"
- Убедитесь, что переменная окружения установлена: `$env:OPENROUTER_KEY`
- Проверьте, что ключ правильный

### Ошибка: "GOOGLE_APPLICATION_CREDENTIALS не установлен"
- Установите переменную окружения
- Проверьте путь к файлу или JSON строку

### Ошибка при тесте OpenRouter: "401 Unauthorized"
- Проверьте правильность API ключа
- Убедитесь, что ключ активен на https://openrouter.ai/

### Ошибка при тесте Sheets: "Permission denied"
- Убедитесь, что Google Sheet поделена с email Service Account
- Дайте права "Редактор"
- Проверьте email в JSON файле credentials

### GitHub Actions не запускается
- Проверьте `GITHUB_TOKEN` в `apps-script/config.gs`
- Убедитесь, что токен имеет права `repo`
- Проверьте логи Apps Script: View → Logs

