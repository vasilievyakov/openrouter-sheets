# ⚠️ Важно: OPENROUTER_KEY в неправильном месте!

## Проблема:

На скриншоте видно:
- ✅ `GOOGLE_APPLICATION_CREDENTIALS` - в **Repository secrets** (правильно!)
- ❌ `OPENROUTER_KEY` - в **Environment secrets** (неправильно!)

## Почему это проблема:

Workflow использует `${{ secrets.OPENROUTER_KEY }}`, что работает только для **Repository secrets**, а не для Environment secrets.

## 🔧 Как исправить:

### Вариант 1: Переместить в Repository secrets (рекомендуется)

1. Откройте секрет `OPENROUTER_KEY` в Environment secrets
2. Скопируйте значение (нажмите на карандаш для редактирования)
3. Удалите секрет из Environment secrets (корзина)
4. Нажмите **"New repository secret"** (зеленая кнопка)
5. Заполните:
   - **Name:** `OPENROUTER_KEY`
   - **Secret:** вставьте скопированное значение
6. Нажмите **Add secret**

### Вариант 2: Использовать Environment в workflow

Можно изменить workflow, чтобы использовать Environment secrets, но это сложнее.

## ✅ После исправления:

Должно быть:
- ✅ `OPENROUTER_KEY` - в **Repository secrets**
- ✅ `GOOGLE_APPLICATION_CREDENTIALS` - в **Repository secrets**

Оба секрета должны быть в одной секции "Repository secrets"!

## 🚀 После перемещения:

1. Попробуйте запустить workflow снова
2. Он должен найти `OPENROUTER_KEY` и продолжить работу

